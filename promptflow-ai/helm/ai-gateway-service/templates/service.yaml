{{/* helm/ai-gateway-service/templates/service.yaml */}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-gateway-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-gateway-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-gateway-service.selectorLabels" . | nindent 4 }}
